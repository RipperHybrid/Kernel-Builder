name: Branch Run Deleter

on:
  push:
    branches:
      - 'Test-2'
  schedule:
    - cron: '0 0 * * *'  # Runs every day at midnight UTC
  workflow_dispatch:

jobs:
  delete-runs:
    runs-on: ubuntu-latest
    steps:
      - name: Get all runs for Test-2 branch
        id: get_runs
        run: |
          ALL_RUNS=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}/actions/runs")
          TEST_2_RUNS=$(echo "$ALL_RUNS" | jq -r '.workflow_runs[] | select(.head_branch == "Test-2" and .id != env.CURRENT_RUN_ID) | .id')
          echo "Found Test-2 branch runs: $TEST_2_RUNS"
          echo "::set-env name=TEST_2_RUNS::$TEST_2_RUNS"

      - name: Delete Test-2 branch runs
        if: env.TEST_2_RUNS != ''
        run: |
          for run_id in ${TEST_2_RUNS//\\n/ }; do
            curl -X DELETE -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}/actions/runs/$run_id"
            echo "Deleted run with ID: $run_id"
          done
